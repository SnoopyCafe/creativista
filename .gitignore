.DS_Store
node_modules/
.impeccable/
.vercel/

# Site backups: contain wp-config.php with live DB credentials. Never commit.
*.tar.gz
qjfjxcmy*/
old/

# DB dumps: may contain user emails, password hashes, form submissions. Never commit.
*.sql
.vercel

# Referrer capture logs are generated at runtime and may contain visitor data.
ref/data/*.csv
ref/data/*.log
